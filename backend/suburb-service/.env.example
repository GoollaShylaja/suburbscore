# Database
DB_USERNAME=suburbscore
DB_PASSWORD=suburbscore123

# JWT (must match the secret used by user-service to issue tokens)
JWT_SECRET=your-256-bit-secret-minimum-32-characters-long-here

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Swagger (set to true during development only)
SWAGGER_ENABLED=false
